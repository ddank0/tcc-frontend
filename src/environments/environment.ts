export const environment = {
  production: false,
  // localhost, não "api": quem faz a requisição é o navegador na máquina
  // do usuário, não o container do frontend.
  apiUrl: 'http://localhost:8000/api',
};
